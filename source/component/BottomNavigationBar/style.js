import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    box: {
        position: 'absolute',
        left: 0,
        bottom: 0,
        width: '100%',
        zIndex: 999,
    },
});

export default style;